export type LogInCredentials = {
  email: string;
  password: string;
};

export type SignUpCredentials = {
  userName: string;
  firstName: string;
  lastName: string;
  email: string;
  password: string;
  phoneNumber: string;
  admin: boolean;
  totaalToto: boolean;
  teamId: number | '';
};
